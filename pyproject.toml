[project]
name = "kuzushiji-atlas"
version = "0.0.1"
description = "Character-shape dataset of pre-modern Japanese writing, built from open transcriptions aligned to open page images"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.28",
    "pillow>=11",
    "pyyaml>=6",
    "pyarrow>=21",
    "pydantic>=2.11",
    "typer>=0.16",
    "uvicorn[standard]>=0.34",
]

[project.scripts]
atlas = "kuzushiji_atlas.cli:app"

[project.optional-dependencies]
data = ["odfpy>=1.4"]
ocr = ["onnxruntime-gpu>=1.22", "numpy>=2", "opencv-python-headless>=4.11"]
# T21: RT-DETR through transformers, export to ONNX and the parity check. torch 2.14 is the first
# build with a cp314 wheel that runs on the Blackwell card (CUDA 13.0 wheels); see
# models/detector/SPIKE.md.
detector = [
    "torch>=2.14",
    "torchvision>=0.29",
    "transformers>=5.17",
    "onnx>=1.20",
    "onnxscript>=0.7",
    "onnxruntime-gpu>=1.22",
    "numpy>=2",
    # the RT-DETR Hungarian matcher
    "scipy>=1.16",
]

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.12", "cffconvert>=2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kuzushiji_atlas"]

[tool.ruff]
line-length = 110
target-version = "py312"
